import '../helper/helper.dart';
import 'stateless_view.dart';
import '../screen_controller/main_controller.dart';

class MainView extends StatelessView<Main, MainController> {
  const MainView(MainController state, {Key? key}) : super(state, key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
